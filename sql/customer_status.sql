CREATE TABLE customer_status (
                                 id INT NOT NULL AUTO_INCREMENT,
                                 status VARCHAR(50),
                                 owner VARCHAR(50),
                                 PRIMARY KEY (id)
);